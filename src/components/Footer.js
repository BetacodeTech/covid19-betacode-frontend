import React from "react";

const Footer = () => {
    return (
        <div style={styles.footer}>
            <h3>Footer</h3>
        </div>
    );
}

const styles = {
    footer: {
        position: "fixed",
        color: "red"
    }
}

export default Footer;
